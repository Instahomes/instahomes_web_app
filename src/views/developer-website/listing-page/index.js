import React from "react";
import Base from "../../../components/developer-admin/base";
import { withTheme } from "styled-components";

const Listing = () => {
  return (
    <React.Fragment>
      <Base headerName="Add a New Listing"></Base>
    </React.Fragment>
  );
};

export default Listing;
